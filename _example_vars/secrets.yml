---
# IMPORTANT:
# For real data encrypt this file with ansible-vault instead of keeping it plain.
secrets_db_password: "my-super-secret-db-password"
secrets_gandi_api_key: "gandi-live-dns-key"
secrets_db_url: "ecto://postgres:{{ secrets_db_password }}@localhost/{{ elixir_app.name }}_prod"
secrets_secret_key_base: "asecretkeybase"
