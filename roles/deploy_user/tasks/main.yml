---
- name: Create deploy user
  user: name={{ deploy.name }} shell={{ deply_user.shell }}

- name: Add pubkey to deploy
  authorized_key: user={{ deploy.name }}
                  key="{{ lookup('file', '{{ deploy.pubkey }}' ) }}"
                  exclusive=yes
                  manage_dir=yes

- name: Add deploy script
  template: src=deploy.sh.j2
            dest="/usr/local/bin/{{ elixir_app.name }}_deploy.sh"
            owner=root
            group=root
            mode=0755

- name: Allow deploy user to sudo commands for deployment only
  template:
    src: "deploy.sudoers.j2"
    dest: /etc/sudoers.d/{{ deploy.name }}
    validate: 'visudo -cf %s'
